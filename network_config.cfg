% Настройка сетевого оборудования

var MAXUSERS := 100;

DEVICE = {
    ID = 1
    MODE = 0
    LIMITS = {
        MAXUSERS = |MAXUSERS|
        MINUSERS = 1
    }
    PORTS = (8080, 8081, 8082)
}