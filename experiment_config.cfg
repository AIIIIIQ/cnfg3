--[[ Параметры физического эксперимента ]]

var INITIALVALUES := (0, 0, 9.8);

EXPERIMENT = {
    DURATION = 120
    SAMPLERATE = 1000
    INITIALVALUES = |INITIALVALUES|
    SETTINGS = {
        MODE = 1
        SENSORS = {
            COUNT = 3
            TYPES = (1, 2, 3)
        }
    }
}